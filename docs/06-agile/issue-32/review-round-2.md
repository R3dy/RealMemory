# Plan Review — Issue #32, Round 2

**Reviewer:** Anymake Plan Reviewer (fresh context — round 2)
**Plan:** `PROJECTS/realmemory/repo/docs/06-agile/issue-32/plan.md` @ In Review (round 2)
**Issue:** https://github.com/R3dy/RealMemory/issues/32 — Synthetic-brain Phase 1: ReflexCache + tool.execute.before warn inhibition (`type:feature`)
**Code state checked:** `9444aaf` (main, v0.6.0, post-issue-#30 hook probe) — round 1 verified the file:line claims against this commit; the round-2 revisions are confined to §4.2, §4.3, §4.6, §6, §8, §9 (A32.0 added, A32.2 criteria updated), §11, and the Review Log — none of which alter the code-state evidence underpinning dimensions 1–3.
**Location:** `PROJECTS/realmemory/repo/docs/06-agile/issue-32/review-round-2.md`

---

## Round-1 comment resolution

| Comment | Required change | Round-2 status | Evidence |
|---------|-----------------|----------------|----------|
| **1-C1** — INV-017 amendment is a contradiction; superseding ADR required | Commit to ADR-010 before execution (not conditional); update §6 classification; add conflict-gate outcome | **FIXED** | §4.6 now commits unconditionally to writing ADR-010 (`docs/adr/ADR-010-two-pathway-constraint.md`) as the FIRST execution step, gated through the Product Owner Proxy (autonomous mode, gate type `intent-conflict`). §6 classification is "Additive with one superseding ADR (ADR-010)." New story A32.0 makes ADR-010 a prerequisite for A32.1 and A32.2 ("no code is written until ADR-010 is committed"). §6 conflict-gate outcome documented: "The plan does not proceed to execution until ADR-010 is committed." All four required ADR contents present in §4.6 (names INV-017; two-pathway constraint; justification via design doc §3; Product Owner Proxy gate). |
| **1-C2** — Race condition: chat.message overwrites warn notes | Use a separate field (e.g. `pendingWarnNote`) or acknowledge the race | **FIXED** | §4.2 adds `state.pendingWarnNote: string \| null` as a separate field (PluginState additions block). §4.3 modifies `experimental.chat.system.transform` to deliver both fields (warn note pushed, then nulled independently). §8 blast-radius row for the transform hook now names the race explicitly and states the separate-field resolution. A32.2 criterion added: "Race-free delivery test: `pendingWarnNote` survives a `chat.message` recall overwrite of `pendingInjection`. Both delivered by transform hook." Experience Script step 5 asserts `state.pendingWarnNote` contains the warn note. |
| Minor — function name `createPlugin` → `realmemoryPlugin` | Use the correct name | **FIXED** | §3 "Current-State Review" reads "The `realmemoryPlugin` function (default export, `src/plugin.ts:217`)." |
| Minor — tsup entry hedge | Reword to "bundled transitively, no entry change" | **FIXED** | §4.7: "`src/reflex.ts` is an internal module imported by `src/plugin.ts` → bundled transitively by `src/plugin-entry.ts`… No tsup entry point change needed." A32.2 criterion reworded to "Verify `src/reflex.ts` is bundled transitively via `plugin.ts` → `plugin-entry.ts` — no tsup entry point change needed." |

Both round-1 comments and both minor notes are resolved. No push-back without rationale.

---

## Checklist

Every dimension gets a result. FAIL requires a numbered comment below.

| # | Dimension | Result | Evidence |
|---|-----------|--------|----------|
| 1 | **Root cause verified** — the file:line trace in plan §2/§3 was checked against the actual code and motivation is real (feature) | PASS | Round 1 verified at `9444aaf`: `src/plugin.ts` returns 5 hook handlers at line 282 with `tool.execute.before` absent; `src/hook-probe.ts:38` `CONDITIONAL_HOOKS` omits `tool.execute.before`. Round-2 revisions do not alter these claims. Design doc §2 table confirms all four writable behavioral gates unused. Motivation (first inhibition gate, advisory `warn`) is real and matches design doc §4.3. |
| 2 | **Solves the reported issue** — plan §4 demonstrably resolves the §1 problem statement | PASS | §4.1 builds `src/reflex.ts` (`ReflexCache`, `compileRule`, `buildReflexCache`, `matchCall`, `emptyReflexCache`) matching design doc §3.1. §4.2 wires `tool.execute.before` with `warn`-only action — queues note into `pendingWarnNote` (separate field), does NOT mutate args or throw — matching design doc §4.3 `warn` grade. §4.3–4.5 wire delivery, config, hook probe. Matches design doc §5 Phase 1 row. |
| 3 | **Scope matches the issue** — nothing built beyond what the issue needs | PASS | `rewrite`/`block` excluded (`ReflexRule.action = "warn"` only; config `inhibition: "off" \| "warn"` only). `permission.ask`/`chat.params`/`tool.definition` excluded. Prediction error (Phase 2) excluded. Working-memory window (Phase 3) excluded. `arousal` field is a stub (always 0) — forward-looking type field, not scope creep. `preferences` array populated but unused in Phase 1 — harmless forward-looking structure matching design doc §3.1. No "while we're in here." |
| 4 | **Intent consistency** — §6 classification is correct; no Active Decision or invariant contradicted without a resolved conflict gate | PASS | §6 classification is "Additive with one superseding ADR (ADR-010)." ADR-008 (plugin role/boundary): preserved — new hook + new module within the plugin boundary. ADR-009 (plugin entry + distribution): preserved — `reflex.ts` bundled transitively, no entry change, `dist/` rebuilt (INV-019). INV-014 (3-dep cap + zod): preserved — pure TypeScript, no new deps. INV-017 (non-blocking hooks): superseded by ADR-010 via the conflict gate (Product Owner Proxy, autonomous mode, gate type `intent-conflict`) — the two-pathway constraint (deliberative-path: detached, unbounded; reflex-path: synchronous, <5ms, cache-only) is documented and committed as the first execution step (A32.0). INV-019 (dist committed): preserved. Conflict-gate outcome stated: plan does not proceed until ADR-010 is committed. |
| 5 | **Design consistency** — §7 complete for UI-touching changes | N/A | No UI changes. `reflex_fire` metrics visible via existing `get_metrics` MCP tool and `/api/metrics` endpoint — no new UI. Correctly marked N/A. |
| 6 | **Blast radius honest** — §8 names the real shared paths; protections exist | PASS | §8 names plugin load path (new handler synchronous, cache-only, no throw path), config defaults (nested `brain` merge — handler defaults active via `!== false` / `!== "off"` checks), session.created (detached buildReflexCache, try/catch, cold cache = safe), experimental.chat.system.transform (now delivers both `pendingInjection` and `pendingWarnNote` — separate fields eliminate the race), hook probe (additive CONDITIONAL_HOOKS row), test suite (543+ regression, 5 EADDRINUSE environmental). The race called out in round-1 1-C2 is now addressed: §8 transform row explicitly states "Two separate fields avoid the race where `chat.message`'s detached recall (assignment at `plugin.ts:617`) overwrites a warn note." |
| 7 | **Stories buildable** — §9 criteria are specific and testable; a Worker could build from these | PASS | A32.0 (ADR-010): 3 criteria — ADR file contents specified, Product Owner Proxy gate, prerequisite ordering. A32.1 (`reflex.ts`): 9 criteria — exports listed by name, `compileRule` logic (command/filePath/user_preference/no-match), `buildReflexCache` search params specified, `matchCall` behavior + cold-cache, latency assertion `<5ms` via `performance.now()`, rule cap, `emptyReflexCache` shape, no new deps. A32.2 (plugin wiring): 16 criteria — handler registration, `recordHookFired` at top, config gates, synchronous `matchCall`, `pendingWarnNote` queue (separate field), transform hook delivers both, session.created cache build, `PluginState` fields, `CONDITIONAL_HOOKS` entry, `MemoryStoreConfig.brain`, `DEFAULTS.brain`, config toggle test, metric recording test, race-free delivery test, doctor report row, transitive bundling, `dist/` rebuild, 543+ regression. A Worker could build from these + the plan alone. |
| 7a | **Experience Script present** — every story has a literal Experience Script or explicit N/A justification | PASS | A32.0: "N/A — documentation artifact, no runtime-verifiable behavior." Justified. A32.1: "N/A — pure logic module with no runtime-verifiable UI/CLI behavior. The Experience Runner will verify the integrated behavior in A32.2." Justified. A32.2: 7-step literal walkthrough — seed lesson memory → load plugin → trigger session.created → call `tool.execute.before` with matching call → assert `state.pendingWarnNote` contains `[realmemory reflex]` and the note → trigger transform → assert `output.system` contains warn note → query `getLatestMetricRow("reflex_fire:")` assert value 1. Action/expected-result steps, not a restatement of criteria. Step 5 asserts the separate `pendingWarnNote` field (race-free). |
| 8 | **Test plan sufficient** — repro becomes a regression test; Experience Script named; blast-radius tests named; no "works correctly" language | PASS | §10 names `tests/reflex.test.ts` (compileRule variants, buildReflexCache sorting/cap/preferences, matchCall match/no-match/cold/latency <5ms, emptyReflexCache), `tests/plugin-reflex.test.ts` (config toggles, match→note, no-match→no-op, metric recording, session.created build, cold cache, race-free delivery), `tests/hook-probe.test.ts` extension (CONDITIONAL_HOOKS + doctor row), `tests/config.test.ts` extension (brain default + override). Experience Script from §9 A32.2 referenced in §10. Regression: 543+ tests, 5 EADDRINUSE flagged as environmental. No "works correctly" language — criteria are specific assertions. |
| 9 | **Rollback complete** — §11 has real branch/revert/migration-down steps, not placeholders | PASS | Branch: `issue/32-reflex-warn-inhibition`. Merge: single squash. Revert: `git revert -m 1 [merge SHA]` (or `git revert [squash SHA]`). Migrations: none (additive metrics rows, in-RAM cache). Intent layer: revert INV-017 amendment + ADR-010 file. Deploy: `npm run build` (tsup) or `git checkout main -- dist/` to rebuild after revert. Version: 0.6.0 → 0.7.0 MINOR (pre-1.0 semver per ADR-004). Real commands, no placeholders. |
| 10 | **Security** — no auth/authz/tenant-isolation/secret/payment surface weakened | PASS | `warn` only — handler does not block, rewrite, or throw. No security surface weakened. A malicious memory with `metadata.command: "npm install"` would cause a false-positive warn (advisory one-line note), not a block — and the user can inspect and `forget` the memory (design doc §4.3 guardrail). INV-001 (secrets scrubbed): ReflexCache reads from the store where memories are already scrubbed — no new unscrubbed path. No ESCALATE needed. |

---

## Comments *(required for every FAIL — each specific and actionable)*

None. All dimensions PASS or N/A.

---

## Verdict

**VERDICT: APPROVED** — all dimensions PASS; near-certainty this plan (1) resolves
the reported issue, (2) breaks nothing in the blast radius, (3) keeps the UI
coherent with the design system, (4) is cleanly revertible.

**Summary:** Round-1 comments 1-C1 (INV-017 contradiction → superseding ADR-010, now committed as prerequisite story A32.0 with conflict-gate outcome documented) and 1-C2 (race condition → separate `pendingWarnNote` field with race-free delivery test and Experience Script assertion) are both fully resolved, along with both minor notes (function name, tsup entry). All 10 dimensions PASS. One non-blocking observation: §11 line 451 retains stale conditional wording ("If a superseding ADR-010 was written (only if reviewer classifies the amendment as a contradiction)") that predates the unconditional commitment in §4.6 — the rollback intent (revert the ADR file) is still clear, but a worker may want to tidy the phrasing during execution. Not blocking.

